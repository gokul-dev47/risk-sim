# Base URL of the backend API.
# Local dev (matches docker-compose.yml / uvicorn --reload default):
VITE_API_URL=http://127.0.0.1:8010
# Production (Vercel/Netlify): set this to your deployed backend, e.g.
# VITE_API_URL=https://risk-sim-backend.onrender.com
